class Solution {
    public int strStr(String haystack, String needle) {
        int i=0;
        int j=needle.length()-1;

        while(j<haystack.length())
        {
String chk=haystack.substring(i,j+1);
if(chk.equals(needle))
{
    return i;
}
i++;
j++;
        }

        return -1;
    }
}
