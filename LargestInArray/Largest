class Solution {
    public static int largest(int[] arr) {
        // code here

        int num=arr[0];
        for(int i=1;i<arr.length;i++)
        {
            if(num<arr[i])
            {
                num=arr[i];
            }
        }

        return num;

    }
}
