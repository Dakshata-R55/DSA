class Solution {
    public double findMaxAverage(int[] nums, int k) {
    double lar=0;
double tem=0;

for(int i=0;i<k;i++)
{
    tem+=nums[i];
}
lar=tem;

for(int i=k;i<nums.length;i++)
{
    tem=tem+nums[i]-nums[i-k];

    lar=Math.max(lar,tem);
}

return lar/k;
    }
}
