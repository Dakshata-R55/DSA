class Solution {
    static boolean searchInSorted(int arr[], int k) {
        // Your code here

        boolean ret =false;

        for(int i=0;i<arr.length;i++)
        {
            if(arr[i]==k)
            {
                ret=true;
            }
        }

        return ret;

    }
}
