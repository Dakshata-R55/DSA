class Solution {
    public int lengthOfLastWord(String s) {

int len=s.length();
int ret=0;
for(int i=len-1;i>=0;i--)
{
    if(s.charAt(i)!=(' '))
    {
ret++;
    }

    if(s.charAt(i)==(' ')&& ret>0)
    {
        break;
    }
}

return ret;


    }
}
